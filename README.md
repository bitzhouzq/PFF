# PFF for infrared-visible image fusion

The proposed perceptual fusion framework (PFF) for infrared and visible images, 
which is described in "Zhou et al., A perceptual framework for infrared-visible image fusion based on multiscale structure decomposition and biological vision".
To implement this algorithm, run "PFF_test.m".
The codes are to be further optimized.
